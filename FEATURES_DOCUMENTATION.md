<div dir="rtl" align="right">

# 📖 توثيق ميزات مشروع Be Star — كن نجماً
## AI-Enhanced Professional Documentation v1.0

> **المستند:** التوثيق الرسمي الشامل لمنصتي مشروع Be Star  
> **التاريخ:** 25 فبراير 2026  
> **الإصدار:** v1.0 — AI Enhanced  
> **الحالة:** مراجعة نهائية  

---

## 📌 ملخص تنفيذي (Executive Summary)

مشروع **Be Star (كن نجماً)** هو نظام رقمي متكامل لإدارة الفعاليات يتألف من منصتين رئيسيتين:

1. **المنصة الرسمية** — واجهة عامة تتيح للجمهور التسجيل وحجز التذاكر والتعرف على محتوى الفعالية
2. **لوحة تحكم الإدارة** — واجهة إدارية شاملة تغطي 11 وحدة وظيفية (التذاكر، VIP، المسابقات، الشهادات، التفاعل المباشر عبر WhatsApp، وغيرها)

يعتمد النظام على بنية **Frontend (React/Vite) + Backend (Python/FastAPI)** مع تكامل مع **Evolution API** لخدمات WhatsApp و**n8n** لأتمتة سير العمل.

### أبرز المؤشرات الحالية:
| المؤشر | القيمة |
|--------|--------|
| إجمالي التذاكر المسجلة | 66 تذكرة |
| تذاكر VIP | 47 |
| تذاكر Student | 19 |
| إجمالي الإيرادات | 16,300 جنيه مصري |
| الأسئلة الفعالة | 18 سؤال |
| المسؤولون النشطون | 3 |
| الموزعون المعتمدون | 4 |

---

## 🏛️ البنية التقنية (Technical Architecture)

### مكونات النظام

| المكون | التقنية | الدور | المنفذ |
|--------|---------|-------|--------|
| المنصة الرسمية | React + Vite | واجهة الجمهور | 3006 |
| لوحة الإدارة | React + Vite | واجهة المسؤولين | 3005 |
| الخادم الخلفي | Python FastAPI | معالجة البيانات والمنطق | 8000 |
| قاعدة البيانات | SQLite (SQLAlchemy ORM) | تخزين البيانات | — |
| خدمة WhatsApp | Evolution API | إرسال الرسائل والشهادات | خارجي |
| أتمتة سير العمل | n8n | الحجز والردود الذكية | خارجي |
| خدمة البريد | Gmail SMTP | إشعارات البريد الإلكتروني | خارجي |
| النشر | Docker Compose | حاويات + Nginx | — |

### مخطط تدفق البيانات

```
┌─────────────────────────────────────────────────────────────┐
│                        المستخدمون                            │
└──────────────┬──────────────────────┬───────────────────────┘
               │                      │
     ┌─────────▼─────────┐  ┌────────▼──────────┐
     │  المنصة الرسمية    │  │   لوحة الإدارة     │
     │  :3006             │  │   :3005            │
     │  (React + Vite)    │  │   (React + Vite)   │
     └─────────┬─────────┘  └────────┬──────────┘
               │                      │
               └──────────┬───────────┘
                          │ REST API
                ┌─────────▼──────────┐
                │   FastAPI Backend   │
                │   :8000             │
                │                     │
                │  ┌───────────────┐  │
                │  │ 13 API Route  │  │
                │  │ Files         │  │
                │  └───────────────┘  │
                │  ┌───────────────┐  │
                │  │ 6 Services    │  │
                │  └───────────────┘  │
                └─┬──────┬──────┬────┘
                  │      │      │
          ┌───────▼┐  ┌──▼───┐  ┌▼────────┐
          │ SQLite │  │ WA   │  │ Gmail   │
          │ DB     │  │ API  │  │ SMTP    │
          └────────┘  └──┬───┘  └─────────┘
                         │
                    ┌────▼────┐
                    │  n8n    │
                    │Workflow │
                    └─────────┘
```

---

## 📑 قاموس المصطلحات (Glossary)

| المصطلح | المعنى |
|---------|--------|
| **VIP** | ضيف مميز — تذكرة بسعر أعلى وامتيازات خاصة |
| **Student** | تذكرة طلابية — بسعر مخفض |
| **payment_submitted** | حالة التذكرة: العميل رفع إثبات الدفع وينتظر الموافقة |
| **approved** | حالة التذكرة: تمت الموافقة من الأدمن |
| **activated** | حالة التذكرة: تم تفعيلها عند دخول الفعالية |
| **rejected** | حالة التذكرة: تم رفضها (إثبات دفع غير صحيح مثلاً) |
| **Anti-Ban Delay** | نظام تأخير عشوائي بين رسائل WhatsApp لتجنب حظر الرقم |
| **Batch** | دُفعة — مجموعة من 25 رسالة تُرسل متتالية ثم فترة راحة |
| **Fuzzy Matching** | مطابقة ضبابية — مقارنة نصوص بنسبة تشابه (ليست حرفية) |
| **n8n** | أداة أتمتة مفتوحة المصدر لربط الخدمات ببعضها |
| **Evolution API** | خدمة توفر API للتواصل عبر WhatsApp |
| **Webhook** | نقطة استقبال HTTP يُرسل إليها إشعار عند حدوث حدث |

---

# 🌐 المنصة الأولى: المنصة الرسمية (Official Event Platform)

> **الرابط:** `https://platform-bestar.mrailabs.com`  
> **المجلد:** `Be Star Platform/`  
> **حالة الاختبار:** ✅ تم الاختبار الشامل — جميع الصفحات تعمل

---

### 1.1 🏠 الصفحة الرئيسية (Home)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | نقطة الدخول الأولى للزائر — تعرض الهوية البصرية للفعالية وتوفر نقاط وصول سريعة لجميع الأقسام |
| **القيمة التشغيلية** | أول انطباع يأخذه الزائر — تُحوّل الزوار إلى مسجلين عبر أزرار الحجز المباشرة |
| **المحتوى** | عنوان الفعالية، وصف مختصر، التاريخ والمكان، أزرار توجيهية |
| **الملف** | `pages/Home.jsx` |

---

### 1.2 📋 من نحن (About)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | صفحة تعريفية بالفعالية ورؤيتها وأهدافها |
| **القيمة التشغيلية** | بناء الثقة مع الزائر وإبراز مصداقية الفريق المنظم |
| **الملف** | `pages/About.jsx` |

---

### 1.3 🎤 المتحدثون (Speakers)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | عرض بطاقات المتحدثين (الصورة، الاسم، التخصص) |
| **القيمة التشغيلية** | تحفيز التسجيل عبر عرض أسماء المتحدثين المؤثرين |
| **ملاحظة** | يحتوي حالياً على نصوص نائبة (Placeholder) — يحتاج تحديث البيانات النهائية |
| **الملف** | `pages/Speakers.jsx` |

---

### 1.4 📅 الجلسات (Sessions)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | جدول الجلسات مع فلاتر (محاضرة، ورشة عمل، إلخ) |
| **القيمة التشغيلية** | يساعد المشارك في التخطيط لحضوره |
| **الملف** | `pages/Sessions.jsx` |

---

### 1.5 🤝 الرعاة (Sponsors)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | عرض شعارات الشركات الراعية |
| **القيمة التشغيلية** | تقدير الرعاة وبناء المصداقية |
| **ملاحظة** | يحتوي على أسماء نائبة (Placeholder) — يحتاج تحديث |
| **الملف** | `pages/Sponsors.jsx` |

---

### 1.6 🎫 حجز التذاكر (Tickets)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | صفحة الحجز الرسمية — وسيلتان للحجز: عبر الذكاء الاصطناعي (WhatsApp) أو فورم مباشر |
| **القيمة التشغيلية** | القناة الأساسية لتحويل الزوار إلى مشتركين |
| **الملف** | `pages/Tickets.jsx` |

**حقول الفورم:**
| الحقل | نوعه | مطلوب؟ |
|-------|------|--------|
| الاسم الكامل | نص | ✅ |
| رقم الهاتف | نص | ✅ |
| البريد الإلكتروني | بريد إلكتروني | اختياري |
| نوع التذكرة | قائمة (Student / Standard / VIP) | ✅ |
| طريقة الدفع | قائمة (Vodafone Cash / Instapay / ...) | ✅ |
| إثبات الدفع | رفع ملف (صورة) | ✅ |

**تدفق الحجز التفصيلي:**
```
الزائر يدخل صفحة التذاكر
      │
      ├──► يختار "حجز عبر الذكاء الاصطناعي"
      │         └──► يتحول لواتساب ← n8n يتعامل مع الحجز
      │
      └──► يختار "حجز من خلال فورم"
                └──► يملأ البيانات + يرفع إثبات الدفع
                └──► يضغط "احجز"
                └──► Backend يسجل التذكرة (حالة: payment_submitted)
                └──► رسالة واتساب تأكيدية للعميل
                └──► إشعار بريد إلكتروني للأدمن
                └──► التذكرة تظهر في لوحة الإدارة للمراجعة
```

---

### 1.7 🤖 ميزات الذكاء الاصطناعي (AI Features)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | صفحة تسويقية للميزات التقنية المتقدمة (المساعد الذكي، الحجز الذكي، التقييم) |
| **القيمة التشغيلية** | إبراز التفوق التقني للمشروع |
| **الملف** | `pages/AIFeatures.jsx` |

---

### 1.8 💬 المساعد الذكي (Chat Widget)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | فقاعة دردشة تفاعلية تظهر في كل صفحات المنصة |
| **القيمة التشغيلية** | دعم فوري 24/7 — يجيب على أسئلة الزوار بدون تدخل بشري |
| **آلية العمل** | الزائر يكتب سؤال ← `routes/chat.py` يسجل الرسالة ← n8n Workflow يعالجها عبر AI Agent ← الرد يعود للشات |
| **الملفات** | `components/ChatWidget.jsx` (واجهة) + `routes/chat.py` (API) |

---

### 1.9 🧭 عناصر التنقل

| المكون | الملف | الوصف |
|--------|------|-------|
| **شريط التنقل العلوي** | `components/Navbar.jsx` | روابط لجميع صفحات المنصة + قائمة hamburger للموبايل |
| **ذيل الصفحة** | `components/Footer.jsx` | معلومات التواصل وحقوق النشر |

---

# 🎛️ المنصة الثانية: لوحة تحكم الإدارة (Admin Dashboard)

> **الرابط:** `https://admin-bestar.mrailabs.com`  
> **المجلد:** `admin-frontend/` + `admin-backend/`  
> **حالة الاختبار:** ✅ تم الاختبار الشامل — جميع الـ 11 قسم تعمل

---

### 2.1 🔐 تسجيل الدخول (Login)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | بوابة الدخول الآمنة — مصادقة بالبريد الإلكتروني وكلمة المرور |
| **الآلية** | JWT Token (HS256) — صلاحية 30 يوم |
| **الملفات** | `pages/Login.jsx` + `routes/auth.py` |

---

### 2.2 📊 لوحة الإحصائيات (Dashboard)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | شاشة النظرة العامة — إحصائيات فورية لكل مؤشرات المشروع |
| **الملفات** | `pages/Dashboard.jsx` + `routes/stats.py` |

**المؤشرات المعروضة:**
| المؤشر | الوصف |
|--------|------|
| إجمالي التذاكر | عدد كل التذاكر في النظام |
| تذاكر VIP | عدد تذاكر كبار الزوار |
| تذاكر Student | عدد تذاكر الطلاب |
| الإيرادات | مجموع أسعار التذاكر المعتمدة والمفعلة |
| العملاء | عدد العملاء المسجلين |
| رسم بياني دائري | توزيع حالات التذاكر بصرياً |
| آخر 10 تذاكر | جدول بأحدث الحجوزات |

---

### 2.3 👑 إدارة كبار الزوار (VIP Guests)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | إدارة قائمة ضيوف VIP — دعوات خاصة وتتبع ردود الحضور |
| **القيمة التشغيلية** | ضمان تجربة مميزة للضيوف المهمين مع تتبع حالاتهم |
| **الملفات** | `pages/VipGuests.jsx` + `routes/vip.py` |

**حالات الضيف VIP:**
| الحالة | المعنى |
|--------|--------|
| مدعو (invited) | تم إرسال الدعوة عبر واتساب |
| مؤكد (confirmed) | الضيف أكد حضوره |
| حاضر (attended) | حضر الفعالية فعلياً |
| معتذر (declined) | اعتذر عن الحضور |

**الميزات:**
- إضافة ضيوف VIP بالاسم ورقم الواتساب
- إرسال دعوات واتساب مخصصة
- ردود تلقائية (قبول/رفض عبر واتساب)
- تخصيص نصوص الترحيب والقبول والرفض

---

### 2.4 🎫 إدارة التذاكر (Tickets)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | المركز الرئيسي لمراجعة وإدارة كل الحجوزات |
| **القيمة التشغيلية** | الأدمن يراجع إثباتات الدفع ويقبل/يرفض التذاكر من هنا |
| **الملفات** | `pages/Tickets.jsx` + `pages/PaymentProof.jsx` + `routes/tickets.py` |

**الميزات:**
| الميزة | الوصف |
|--------|------|
| البحث | بالاسم، الكود، أو رقم الهاتف |
| التصفية | حسب الحالة (الكل / في المراجعة / معتمد / مرفوض) |
| عرض إثبات الدفع | صفحة مستقلة (`/tickets/:id/proof`) بصورة كبيرة + أزرار قبول/رفض |
| العودة التلقائية | بعد القبول/الرفض ← شاشة نجاح ← عودة تلقائية خلال 1.5 ثانية |
| تحميل PDF | للتذاكر المعتمدة — يتضمن الكود والاسم والنوع والسعر |
| إشعار واتساب | رسالة تلقائية للعميل عند الموافقة أو الرفض |

**دورة حياة التذكرة:**
```
new → payment_submitted → approved → activated
                       └── rejected
```

| المرحلة | الوصف | من يبدأها |
|---------|------|----------|
| `payment_submitted` | العميل رفع إثبات الدفع | النظام (تلقائي) |
| `approved` | الأدمن وافق بعد المراجعة | الأدمن |
| `rejected` | الأدمن رفض (إثبات غير صالح) | الأدمن |
| `activated` | العميل دخل الفعالية فعلياً | النظام (عند المسح) |

---

### 2.5 ✅ قائمة المهام (Checklist)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | أداة تتبع المهام التحضيرية — ضمان عدم نسيان أي متطلب |
| **القيمة التشغيلية** | تنظيم الفريق وتوزيع المسؤوليات قبل الفعالية |
| **الملفات** | `pages/ChecklistPage.jsx` + `routes/checklist.py` |

---

### 2.6 📅 الأجندة (Agenda)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | إدارة الجدول الزمني (جلسات، ورش، استراحات) |
| **القيمة التشغيلية** | التحكم المركزي في برنامج الفعالية |
| **الملفات** | `pages/AgendaPage.jsx` + `routes/agenda.py` |

---

### 2.7 ⚠️ الشكاوى المباشرة (Complaints)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | استقبال وإدارة شكاوى المشاركين أثناء الفعالية |
| **القيمة التشغيلية** | معالجة المشكلات فوراً لضمان رضا المشاركين |
| **الإحصائيات الحالية** | 4 شكاوى إجمالي — 1 مفتوحة، 3 تم حلها |
| **الملفات** | `pages/ComplaintsPage.jsx` + `routes/complaints.py` |

---

### 2.8 👥 الموزعون (Distributors)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | إدارة فريق بيع التذاكر الميداني |
| **القيمة التشغيلية** | تتبع مبيعات كل موزع ومحاسبتهم |
| **العدد الحالي** | 4 موزعين مسجلين |
| **الملفات** | `pages/Distributors.jsx` + `routes/distributors.py` |

---

### 2.9 🛡️ المسؤولون (Admins)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | إدارة صلاحيات الوصول — إضافة/تعديل/حذف مسؤولين |
| **القيمة التشغيلية** | التحكم في من يدخل لوحة الإدارة |
| **العدد الحالي** | 3 مسؤولين |
| **الملفات** | `pages/Admins.jsx` + `pages/AddAdmin.jsx` + `pages/EditAdmin.jsx` + `routes/auth.py` |

---

### 2.10 📡 التفاعل المباشر (Live Engagement)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | نظام إرسال رسائل واتساب جماعية للمشاركين |
| **القيمة التشغيلية** | التواصل الفوري مع جميع المشاركين (إعلانات، توجيهات، تفاعل) |
| **العدد الحالي** | 39 حاضر مسجل |
| **الملفات** | `pages/LiveEngagement.jsx` + `routes/engagement.py` |

**أنواع الرسائل:** نصية | صورة مع تعليق | دعوة | رابط

**🛡️ نظام Anti-Ban (حماية الرقم من الحظر):**

| المعيار | القيمة |
|---------|--------|
| التأخير بين الرسائل | 3–8 ثوانٍ عشوائية |
| حجم الدُفعة | 25 رسالة |
| الراحة بين الدُفعات | 60–90 ثانية |
| تخصيص المحتوى | اسم المشارك + كود التذكرة في كل رسالة |
| نمط التنفيذ | في الخلفية (Background) — الـ API ترد فوراً |

---

### 2.11 🎯 محرك المسابقات (Quiz Engine)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | نظام مسابقات تفاعلي — أسئلة تُرسل عبر واتساب والمشاركون يردون |
| **القيمة التشغيلية** | تفاعل حي يزيد مشاركة الجمهور ويكسر الجمود |
| **العدد الحالي** | 18 سؤال فعال |
| **الملفات** | `pages/QuizEngine.jsx` + `routes/quiz.py` + `services/scoring.py` |

**أنواع الأسئلة:**
| النوع | آلية الإجابة | آلية التقييم |
|-------|-------------|-------------|
| اختيار من متعدد (MCQ) | يرد بحرف (A/B/C/D) | مطابقة حرفية |
| إكمال (Completion) | يكتب الإجابة | مطابقة ضبابية (Fuzzy) — عتبة 90% |

**محرك التقييم الذكي (`scoring.py`):**
- تطبيع النص العربي (إزالة التشكيل، توحيد الهمزات/التاء المربوطة/الياء)
- مقارنة متعددة الاستراتيجيات: `ratio` + `partial_ratio` + `token_sort` + `token_set`
- دعم إجابات متعددة صحيحة (مفصولة بـ `||`)

**تدفق المسابقة:**
```
الأدمن ينشئ سؤال + يحدد الإجابة + الوقت + النقاط
      │
      ▼
يختار المجموعة المستهدفة (الكل / VIP / Student / مخصصة)
      │
      ▼
يضغط "إرسال" ← السؤال يُرسل لكل الهواتف (مع delay)
      │
      ▼
المشارك يرد عبر واتساب ← n8n يستقبل ← Quiz API يقيّم
      │
      ▼
النتيجة تظهر في Leaderboard + نقاط إضافية للسرعة
```

---

### 2.12 🏆 الشهادات والشكر (Certificates & Thanks)

| البند | التفاصيل |
|-------|---------|
| **الوصف** | إصدار شهادات تقدير PDF + إرسال رسائل شكر مخصصة |
| **القيمة التشغيلية** | تكريم المشاركين وإرسال شهادات رسمية بعد الفعالية |
| **الملفات** | `pages/CertificatesPage.jsx` + `routes/certificates.py` + `services/certificate_generator.py` |

**الوحدات الفرعية:**
| الوحدة | الوظيفة |
|--------|---------|
| تحليل المشاركين | جدول بالنقاط والترتيب والمركز |
| إرسال الشهادات | PDF يُولد لكل مشارك ← يُرسل عبر واتساب |
| رسائل الشكر | رسائل مخصصة بمتغيرات: `{name}` `{points}` `{rank}` |
| سجل الشهادات | تاريخ كل شهادة (نجاح/فشل) |
| سجل الشكر | تاريخ كل رسالة شكر |

> ⚠️ جميع عمليات الإرسال تستخدم نظام Anti-Ban (delay + batching)

---

## 🔌 الخدمات المساندة (Backend Services)

### خدمة WhatsApp (`services/whatsapp_service.py`)
| الوظيفة | الوصف |
|---------|------|
| `send_message()` | إرسال رسالة نصية |
| `send_image()` | إرسال صورة مع تعليق |
| `send_link()` | إرسال رابط بعنوان ووصف |
| `send_certificate()` | إرسال ملف PDF |

### خدمة البريد (`services/email_service.py`)
| الوظيفة | الوصف |
|---------|------|
| `send_ticket_email()` | بريد تأكيد التذكرة (HTML مصمم + PDF مرفق) |
| `send_admin_notification()` | إشعار الأدمن بحجز جديد |

### مولدات PDF
| الخدمة | الملف | المخرج |
|--------|------|-------|
| مولد التذاكر | `services/pdf_generator.py` | PDF بكود التذكرة + الاسم + النوع + السعر |
| مولد الشهادات | `services/certificate_generator.py` | PDF بالاسم + النقاط + المركز |

### محرك التقييم (`services/scoring.py`)
تقييم إجابات المسابقات بدعم كامل للغة العربية (تطبيع + مطابقة ضبابية)

### عميل n8n (`services/n8n_client.py`)
- webhook trigger عند موافقة تذكرة
- ربط بين Backend وسير عمل n8n

---

## 🔄 سير العمل الآلي (n8n Workflow)

> **الملف:** `n8n_workflow/webchat_workflow.json`

| التكامل | الوصف |
|---------|------|
| استقبال رسائل واتساب | معالجة رسائل المشاركين والرد الذكي |
| حجز عبر واتساب | إنشاء تذاكر + قبول إثبات الدفع مباشرة عبر الشات |
| أجوبة المسابقات | استقبال الردود وإرسالها للـ Quiz API |
| خدمة VIP | ردود تلقائية لقبول/رفض الدعوات |
| إرسال تذاكر PDF | عند الموافقة → إنشاء PDF → إرسال عبر واتساب |

---

## 🔒 تقرير الأمان والاختبار

### نتائج اختبار المنصة الرسمية
| الاختبار | النتيجة |
|----------|---------|
| تحميل جميع الصفحات (7) | ✅ ناجح |
| نموذج حجز التذاكر | ✅ كل الحقول موجودة |
| ChatWidget | ✅ ظاهر وتفاعلي |
| استجابة الموبايل | ✅ قائمة hamburger تعمل |
| أخطاء JavaScript | ✅ لا توجد (تحذير بسيط: preload logo.png) |

### نتائج اختبار لوحة الإدارة
| الاختبار | النتيجة |
|----------|---------|
| تسجيل الدخول | ✅ ناجح |
| جميع الأقسام (11) | ✅ تحمل البيانات |
| الإحصائيات | ✅ أرقام صحيحة |
| أخطاء JavaScript | ✅ لا توجد أخطاء حرجة |

### ملاحظات أمنية يُوصى بمعالجتها مستقبلاً

| # | المستوى | الملاحظة | التوصية |
|---|---------|----------|---------|
| 1 | 🔴 عالي | CORS مفتوح لجميع المصادر (`allow_origins=["*"]`) | تحديد النطاقات المسموحة فقط |
| 2 | 🔴 عالي | 11 من 13 Route بدون حماية Token | إضافة `Depends(verify_token)` لكل الـ Admin Routes |
| 3 | 🟡 متوسط | نقطة `/api/auth/init` متاحة بدون حماية — تعرض كلمة المرور الافتراضية | تعطيلها أو حمايتها بعد أول استخدام |
| 4 | 🟡 متوسط | `SECRET_KEY` ضعيف وافتراضي | استخدام مفتاح عشوائي قوي في الإنتاج |
| 5 | 🟢 منخفض | بيانات حساسة في `.env` ملتزمة في Git | إضافة `.env` إلى `.gitignore` |

---

## 📁 هيكل المشروع الكامل

```
Be Star/
│
├── Be Star Platform/              ← المنصة الرسمية (7 صفحات + 3 مكونات)
├── admin-frontend/                ← لوحة الإدارة (15 صفحة + Sidebar)
├── admin-backend/                 ← الخادم الخلفي (13 Route + 6 Services)
├── n8n_workflow/                  ← سير العمل (webchat_workflow.json)
├── docker-compose.yml             ← تهيئة Docker
└── FEATURES_DOCUMENTATION.md      ← هذا المستند
```

---

> 📅 آخر تحديث: 25 فبراير 2026  
> ✍️ الإصدار: AI-Enhanced Professional Documentation v1.0  
> 🔧 أدوات التوليد: Antigravity AI Assistant  

</div>
