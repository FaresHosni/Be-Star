# 📖 توثيق ميزات مشروع Be Star — كن نجماً
# Features Documentation — Human Version

> هذا المستند هو مرجع شامل لكل ميزات منصتي مشروع **Be Star** (المنصة الرسمية + لوحة تحكم الأدمن).
> أي شخص جديد يقرأ هذا المستند سيفهم ما يفعله كل قسم وكيف يعمل بدون الحاجة لشرح إضافي.

---

## 🏗️ نظرة عامة على المشروع

مشروع **Be Star (كن نجماً)** هو نظام متكامل لإدارة الفعاليات والإيفنتات، يتكون من:

| المكون | الوصف | التقنية |
|--------|-------|---------|
| **المنصة الرسمية** | الموقع الذي يراه الجمهور ويسجل من خلاله | React (Vite) |
| **لوحة تحكم الأدمن** | واجهة الإدارة للتحكم في كل شيء | React (Vite) |
| **الباك إند** | السيرفر الذي يعالج كل العمليات | Python (FastAPI) |
| **سير العمل** | أتمتة التواصل عبر واتساب والذكاء الاصطناعي | n8n Workflow |
| **خدمة الواتساب** | إرسال رسائل وشهادات وصور عبر WhatsApp | Evolution API |
| **خدمة البريد** | إرسال إيميلات تأكيد التذاكر للعملاء | Gmail SMTP |

### كيف تتواصل المكونات مع بعضها:

```
  ┌──────────────────┐        ┌──────────────────┐
  │   المنصة الرسمية  │        │   لوحة الأدمن     │
  │   (الجمهور)       │        │   (الإدارة)       │
  └───────┬──────────┘        └───────┬──────────┘
          │                           │
          ▼                           ▼
  ┌─────────────────────────────────────────────┐
  │          ⚙️ Backend — FastAPI                │
  │  (tickets, engagement, quiz, certificates)  │
  └────┬───────────┬──────────────┬─────────────┘
       │           │              │
       ▼           ▼              ▼
  ┌────────┐  ┌─────────┐  ┌──────────┐
  │ 📱 WA  │  │ 📧 Email│  │ 🗄️ DB   │
  │Evolution│  │  Gmail  │  │ SQLite/  │
  │  API    │  │  SMTP   │  │PostgreSQL│
  └────────┘  └─────────┘  └──────────┘
       ▲
       │
  ┌────────────┐
  │ 🔄 n8n     │
  │ Workflow   │
  └────────────┘
```

---

# 🌐 المنصة الأولى: المنصة الرسمية (Official Event Platform)

> **المجلد:** `Be Star Platform/`
> هذه المنصة هي **واجهة الجمهور** — الموقع الرسمي الذي يمثل الهوية الرقمية لفعالية "كن نجماً".

---

### 1. 🏠 الصفحة الرئيسية (Home)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | أول صفحة يراها الزائر — تعرض هوية الفعالية ونبذة سريعة عنها |
| **الفائدة التشغيلية** | تجذب الانتباه وتوجه الزائر لاتخاذ إجراء (حجز تذكرة، معرفة المتحدثين، إلخ) |
| **المحتوى** | عنوان الفعالية، وصف مختصر، تاريخ ومكان الفعالية، أزرار توجيهية سريعة |
| **الملف** | `pages/Home.jsx` |

---

### 2. 📋 من نحن (About)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | تعرض معلومات تفصيلية عن الفعالية وأهدافها والفريق المنظم |
| **الفائدة التشغيلية** | تبني ثقة الزائر وتوضح رؤية وأهداف المشروع |
| **الملف** | `pages/About.jsx` |

---

### 3. 🎤 المتحدثون (Speakers)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | تعرض قائمة المتحدثين في الفعالية مع صورهم وألقابهم وتخصصاتهم |
| **الفائدة التشغيلية** | يعرف الجمهور من سيحضر مما يشجعهم على التسجيل |
| **الملف** | `pages/Speakers.jsx` |

---

### 4. 📅 الجلسات (Sessions)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | تعرض جدول الجلسات والمحاضرات مع أوقاتها ومن يقدمها |
| **الفائدة التشغيلية** | يخطط المشارك ليومه ويعرف ما سيحضره |
| **الملف** | `pages/Sessions.jsx` |

---

### 5. 🤝 الرعاة (Sponsors)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | تعرض شعارات وأسماء الشركات الراعية للفعالية |
| **الفائدة التشغيلية** | تقدير للرعاة وبناء مصداقية للفعالية |
| **الملف** | `pages/Sponsors.jsx` |

---

### 6. 🎫 حجز التذاكر (Tickets)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | تسمح للزائر بحجز تذكرة حضور (VIP أو Student) |
| **الفائدة التشغيلية** | قناة الحجز الرسمية — الزائر يكمل الحجز بالكامل من هنا |
| **الملف** | `pages/Tickets.jsx` |

**طريقة الاستخدام خطوة بخطوة:**
1. يفتح الزائر صفحة التذاكر
2. يختار نوع التذكرة (VIP أو Student)
3. يدخل بياناته: الاسم، رقم الهاتف، البريد الإلكتروني
4. يرفع صورة إثبات الدفع (تحويل Vodafone Cash مثلاً)
5. يضغط "احجز" ← التذكرة تنزل في لوحة الأدمن بحالة "في المراجعة"
6. يستلم رسالة واتساب تأكيدية باستلام الطلب

---

### 7. 🤖 ميزات الذكاء الاصطناعي (AI Features)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | صفحة تعرض خدمات الذكاء الاصطناعي المقدمة في الفعالية |
| **الفائدة التشغيلية** | تسويق للميزات التقنية المتقدمة في المشروع |
| **الملف** | `pages/AIFeatures.jsx` |

---

### 8. 💬 شات بوت (Chat Widget)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | فقاعة دردشة تظهر في كل صفحات المنصة — الزائر يسأل ويحصل على رد ذكي |
| **الفائدة التشغيلية** | يساعد الزائر في الأسئلة الشائعة (أسعار التذاكر، مكان الفعالية، إلخ) بدون انتظار رد بشري |
| **كيف يعمل؟** | الزائر يكتب سؤال ← الرسالة تتبعت للـ Backend (`routes/chat.py`) ← تمر على n8n Workflow ← الـ AI يكتب الرد ← الرد يرجع للشات |
| **الملفات** | `components/ChatWidget.jsx` (الواجهة) + `routes/chat.py` (الـ API) |

---

### 9. 🧭 الـ Navbar و Footer

- **Navbar** (`components/Navbar.jsx`): شريط التنقل العلوي — روابط لكل صفحات المنصة
- **Footer** (`components/Footer.jsx`): ذيل الصفحة — معلومات التواصل وحقوق النشر

---

# 🎛️ المنصة الثانية: لوحة تحكم الأدمن (Admin Dashboard)

> **المجلد:** `admin-frontend/` (الواجهة) + `admin-backend/` (السيرفر)
> هذه هي **غرفة التحكم** — يستخدمها المسؤولون والمنظمون لإدارة كل جوانب الفعالية.

---

### 1. 🔐 تسجيل الدخول (Login)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | شاشة دخول بالبريد الإلكتروني وكلمة المرور |
| **الفائدة التشغيلية** | تأمين لوحة التحكم — فقط المسؤولون المعتمدون يدخلون |
| **الملفات** | `pages/Login.jsx` + `routes/auth.py` |

---

### 2. 📊 لوحة التحكم الرئيسية (Dashboard)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | شاشة إحصائيات شاملة — أول ما يراه الأدمن بعد الدخول |
| **الفائدة التشغيلية** | نظرة سريعة على كل أرقام المشروع في مكان واحد |
| **الملفات** | `pages/Dashboard.jsx` + `routes/stats.py` |

**ما تعرضه:**
- إجمالي التذاكر المسجلة
- عدد التذاكر حسب الحالة: **معلقة (pending)** | **في المراجعة (payment_submitted)** | **معتمدة (approved)** | **مرفوضة (rejected)** | **مفعّلة (activated)**
- عدد التذاكر حسب النوع: **VIP** | **Student**
- إجمالي الإيرادات (من التذاكر المعتمدة والمفعلة)
- عدد العملاء المسجلين
- **رسم بياني دائري (Pie Chart):** توزيع حالات التذاكر بصرياً
- **جدول آخر 10 تذاكر** مسجلة في النظام

---

### 3. 👑 كبار الزوار — VIP (VIP Guests)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | إدارة قائمة ضيوف الـ VIP — دعوات خاصة وتتبع الحضور |
| **الفائدة التشغيلية** | ضمان تجربة مميزة لكبار الضيوف وتتبع ردودهم |
| **الملفات** | `pages/VipGuests.jsx` + `routes/vip.py` |

**الميزات:**
- إضافة ضيوف VIP بالاسم ورقم الواتساب
- تتبع حالة كل ضيف: **مدعو** | **مؤكد** | **حاضر** | **معتذر**
- إرسال دعوات واتساب تلقائية
- ردود تلقائية عبر واتساب (قبول/رفض الدعوة)
- تخصيص رسالة الترحيب والقبول والرفض

---

### 4. 🎫 إدارة التذاكر (Tickets)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | عرض ومراجعة جميع التذاكر المسجلة + قبول/رفض التذاكر |
| **الفائدة التشغيلية** | المكان المركزي لمراجعة الحجوزات والتحكم فيها |
| **الملفات** | `pages/Tickets.jsx` + `pages/PaymentProof.jsx` + `routes/tickets.py` |

**الميزات:**
- **البحث:** بالاسم، الكود، أو رقم الهاتف
- **التصفية:** حسب الحالة (الكل / في المراجعة / معتمد / مرفوض)
- **عرض إثبات الدفع:** الضغط على 👁️ يفتح **صفحة مستقلة** تعرض بيانات التذكرة وصورة الدفع بحجم كبير
- **موافقة/رفض:** من صفحة إثبات الدفع — وبعد الإجراء ترجع تلقائياً لقائمة التذاكر
- **تحميل PDF:** للتذاكر المعتمدة — PDF يتضمن كود التذكرة واسم الحاضر ونوعها وسعرها
- **إشعارات واتساب:** رسالة تلقائية للعميل عند الموافقة أو الرفض

**طريقة الاستخدام:**
1. الأدمن يفتح التذاكر ← يصفي حسب "في المراجعة"
2. يضغط 👁️ على تذكرة ← تفتح صفحة إثبات الدفع
3. يشوف صورة التحويل + بيانات العميل
4. يضغط ✅ موافقة أو ❌ رفض
5. شاشة نجاح تظهر ← ترجع تلقائياً لقائمة التذاكر

**تدفق حالات التذكرة:**
```
حجز جديد ──► payment_submitted (في المراجعة)
                    │
              ┌─────┴─────┐
              ▼           ▼
        approved      rejected
        (معتمد)       (مرفوض)
              │
              ▼
        activated (مفعّل — عند دخول الفعالية)
```

---

### 5. ✅ قائمة المهام (Checklist)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | نظام تتبع المهام التحضيرية للفعالية |
| **الفائدة التشغيلية** | تنظيم الفريق وضمان عدم نسيان أي متطلب قبل الفعالية |
| **الملفات** | `pages/ChecklistPage.jsx` + `routes/checklist.py` |

**الميزات:**
- إضافة وحذف مهام
- تحديد مكتمل / غير مكتمل
- تنظيم حسب الأولوية

---

### 6. 📅 الأجندة (Agenda)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | إدارة الجدول الزمني للفعالية (الجلسات والأنشطة) |
| **الفائدة التشغيلية** | تنظيم البرنامج والتحكم في مواعيد الجلسات والمتحدثين |
| **الملفات** | `pages/AgendaPage.jsx` + `routes/agenda.py` |

**الميزات:**
- إضافة / تعديل / حذف جلسات
- تحديد الوقت والمتحدث والوصف لكل جلسة
- عرض البرنامج بترتيب زمني

---

### 7. ⚠️ الشكاوى المباشرة (Complaints)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | استقبال وإدارة شكاوى واقتراحات المشاركين أثناء الفعالية |
| **الفائدة التشغيلية** | التعامل الفوري مع أي مشكلة يواجهها المشاركون |
| **الملفات** | `pages/ComplaintsPage.jsx` + `routes/complaints.py` |

---

### 8. 👥 الموزعين (Distributors)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | إدارة الموزعين المسؤولين عن بيع التذاكر ميدانياً |
| **الفائدة التشغيلية** | تتبع مبيعات كل موزع ومحاسبتهم |
| **الملفات** | `pages/Distributors.jsx` + `routes/distributors.py` |

---

### 9. 🛡️ المسؤولين (Admins)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | إدارة حسابات المسؤولين (إضافة / تعديل / حذف) |
| **الفائدة التشغيلية** | التحكم في من يستطيع الوصول للوحة الأدمن وصلاحياتهم |
| **الملفات** | `pages/Admins.jsx` + `pages/AddAdmin.jsx` + `pages/EditAdmin.jsx` + `routes/auth.py` |

**طريقة الاستخدام:**
1. فتح قائمة المسؤولين
2. الضغط على "إضافة مسؤول" ← إدخال الاسم والبريد وكلمة المرور
3. لتعديل مسؤول ← الضغط على أيقونة التعديل ← تغيير البيانات

---

### 10. 📡 التفاعل المباشر (Live Engagement)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | إرسال رسائل واتساب جماعية للمشاركين أثناء أو قبل الفعالية |
| **الفائدة التشغيلية** | التواصل اللحظي مع المشاركين (تفاعل، إعلانات، توجيهات) |
| **الملفات** | `pages/LiveEngagement.jsx` + `routes/engagement.py` |

**أنواع الرسائل:**
| النوع | الوصف |
|-------|------|
| **نصي (Text)** | رسالة واتساب عادية |
| **صورة (Image)** | صورة مع تعليق |
| **دعوة (Invitation)** | دعوة مع عنوان ووصف ورابط |
| **رابط (Link)** | رابط بعنوان ووصف |

**طريقة الاستخدام:**
1. الأدمن يحدد المشاركين من القائمة (أو يحدد الكل)
2. يختار نوع الرسالة ويكتب المحتوى
3. يضغط "إرسال" ← الـ API ترد فوراً "تم بدء الإرسال"
4. الرسائل تتبعت في الخلفية واحدة واحدة بتأخير عشوائي

**🛡️ نظام حماية ضد الحظر (Anti-Ban):**
- **تأخير عشوائي:** 3-8 ثواني بين كل رسالة
- **وقفة بين الدُفعات:** كل 25 رسالة ← وقفة 60-90 ثانية
- **تخصيص المحتوى:** كل رسالة بتتضاف ليها اسم المشارك وكود تذكرته (👤 الاسم | 🎫 الكود)
- **إرسال في الخلفية:** الأدمن لا ينتظر — الرسائل تكمل لوحدها

**ميزات إضافية:**
- إخفاء حضور معينين من القائمة (Soft Delete)
- تصفية المشاركين حسب نوع التذكرة

---

### 11. 🎯 محرك المسابقات (Quiz Engine)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | إنشاء وإدارة مسابقات تفاعلية أثناء الفعالية عبر واتساب |
| **الفائدة التشغيلية** | تفاعل حي مع المشاركين — أسئلة تُرسل لهواتفهم ويجاوبوا عبر واتساب |
| **الملفات** | `pages/QuizEngine.jsx` + `routes/quiz.py` + `services/scoring.py` |

**أنواع الأسئلة:**
| النوع | كيف يجاوب المشارك | كيف يتم التقييم |
|-------|-------------------|----------------|
| **اختيار من متعدد (MCQ)** | يرد بحرف (A, B, C, D) | مطابقة حرفية |
| **إكمال (Completion)** | يكتب إجابته في رسالة | تشابه لغوي ذكي (Fuzzy Matching) بدعم العربية |

**محرك التقييم الذكي (`scoring.py`):**
- تطبيع النص العربي (إزالة التشكيل، توحيد الهمزات والتاء المربوطة)
- مطابقة ضبابية (Fuzzy Matching) باستخدام مكتبة `rapidfuzz`
- دعم إجابات متعددة صحيحة مفصولة بـ `||`
- عتبة قبول قابلة للتعديل (افتراضي: 90% تشابه)

**طريقة الاستخدام:**
1. الأدمن ينشئ سؤال ← يحدد النوع والإجابة الصحيحة والنقاط والوقت
2. يختار المجموعة المستهدفة (الكل / VIP / Student / مجموعة مخصصة)
3. يضغط "إرسال" ← السؤال يتبعت لكل الهواتف المستهدفة (مع delay)
4. المشاركون يردوا عبر واتساب ← n8n يستقبل الرد ← الـ API تقيّم الإجابة
5. النتائج تظهر في **لوحة الترتيب (Leaderboard)**

**ميزات إضافية:**
- **مجموعات مخصصة:** إنشاء مجموعات من مشاركين محددين
- **مؤقت:** كل سؤال له وقت إجابة ← ينتهي تلقائياً
- **قبول ردود متأخرة:** خيار اختياري لقبول الإجابات بعد انتهاء الوقت (بدون نقاط)
- **لوحة ترتيب:** ترتيب المشاركين حسب النقاط — فلترة حسب النوع أو المجموعة
- **تفاصيل المشارك:** عرض كل إجابات مشارك معين ونقاطه

---

### 12. 🏆 الشهادات ورسائل الشكر (Certificates & Thanks)

| البند | التفاصيل |
|-------|---------|
| **ماذا تفعل؟** | إنشاء وإرسال شهادات تقدير PDF + رسائل شكر مخصصة للمشاركين |
| **الفائدة التشغيلية** | تكريم المشاركين بعد الفعالية وإرسال شهادات رسمية عبر واتساب |
| **الملفات** | `pages/CertificatesPage.jsx` + `routes/certificates.py` + `services/certificate_generator.py` |

**الأقسام:**
| القسم | الوصف |
|-------|------|
| **تحليل المشاركين** | جدول بكل المشاركين مع نقاطهم وترتيبهم ومركزهم |
| **إرسال الشهادات** | اختيار مشاركين ← إنشاء PDF ← إرسال عبر واتساب |
| **رسائل الشكر** | رسائل مخصصة بمتغيرات: `{name}` (الاسم)، `{points}` (النقاط)، `{rank}` (المركز) |
| **سجل الشهادات** | لوج تاريخي بكل الشهادات المرسلة وحالتها (نجح/فشل) |
| **سجل الشكر** | لوج تاريخي بكل رسائل الشكر المرسلة |

**طريقة استخدام الشهادات:**
1. الأدمن يفتح صفحة الشهادات
2. يشوف جدول المشاركين بنقاطهم وترتيبهم
3. يحدد المشاركين اللي يريد يبعتلهم شهادات
4. يضغط "إرسال الشهادات" ← PDF يتولد لكل مشارك ← يتبعت على واتساب

**طريقة استخدام رسائل الشكر:**
1. الأدمن يكتب رسالة الشكر مع المتغيرات
2. يحدد المشاركين
3. يضغط "إرسال" ← الرسائل تتبعت بالتأخير العشوائي

> ⚠️ جميع عمليات الإرسال (شهادات + شكر) تستخدم نظام الدفعات (batch) مع تأخيرات عشوائية لحماية الرقم من حظر واتساب.

---

## 🔌 الخدمات المساندة (Backend Services)

### 📱 خدمة واتساب (`services/whatsapp_service.py`)

| الوظيفة | الوصف |
|---------|------|
| `send_message()` | إرسال رسالة نصية عبر واتساب |
| `send_image()` | إرسال صورة مع تعليق |
| `send_link()` | إرسال رابط بعنوان ووصف |
| `send_certificate()` | إرسال ملف PDF (شهادة) عبر واتساب |

تستخدم **Evolution API** للتواصل مع واتساب. تتطلب Environment Variables:
- `EVOLUTION_API_URL` — عنوان API
- `EVOLUTION_API_KEY` — مفتاح الوصول
- `EVOLUTION_INSTANCE_NAME` — اسم الـ Instance

---

### 📧 خدمة البريد الإلكتروني (`services/email_service.py`)

| الوظيفة | الوصف |
|---------|------|
| `send_ticket_email()` | إرسال بريد تأكيد التذكرة للعميل (HTML مصمم + PDF مرفق) |
| `send_admin_notification()` | إشعار الأدمن بوجود حجز جديد |

تستخدم **Gmail SMTP**. تتطلب:
- `SMTP_USER` — بريد Gmail
- `SMTP_PASSWORD` — كلمة مرور التطبيق
- `ADMIN_EMAIL` — بريد الأدمن للإشعارات

---

### 🎫 مولد PDF التذاكر (`services/pdf_generator.py`)

- إنشاء تذاكر PDF بتصميم مخصص ذهبي/أسود
- دعم كامل للنصوص العربية (باستخدام `arabic_reshaper` + `python-bidi`)
- يتضمن: كود التذكرة، اسم الحاضر، نوع التذكرة، السعر

---

### 🏆 مولد شهادات PDF (`services/certificate_generator.py`)

- إنشاء شهادات تقدير بتصميم احترافي
- يتضمن: اسم المشارك، إجمالي النقاط، المركز، عدد المشاركين الإجمالي

---

### 🧠 محرك التقييم الذكي (`services/scoring.py`)

- تقييم إجابات المسابقات (MCQ + Completion)
- **للاختيار من متعدد:** مطابقة حرفية (A/B/C/D)
- **للإكمال:** مطابقة ضبابية (Fuzzy Matching) مع تطبيع النص العربي
- عتبة قبول: 90% تشابه (قابل للتعديل)

---

### 🔗 عميل n8n (`services/n8n_client.py`)

- يربط الـ Backend بسير عمل n8n
- يُطلق الـ Webhook عند موافقة تذكرة لإرسال PDF عبر واتساب تلقائياً

---

## 🔄 سير العمل الآلي (n8n Workflow)

> **المجلد:** `n8n_workflow/webchat_workflow.json`

| الميزة | الوصف |
|--------|------|
| **استقبال رسائل واتساب** | الـ workflow يستقبل رسائل المشاركين عبر Evolution API |
| **الرد الذكي** | يستخدم AI Agent للرد على أسئلة المشاركين |
| **حجز عبر واتساب** | إنشاء تذاكر وقبول إثبات الدفع عبر واتساب مباشرة |
| **أجوبة المسابقات** | استقبال إجابات المسابقات وإرسالها لـ Quiz API |
| **خدمة VIP** | ردود تلقائية لقبول/رفض دعوات VIP |
| **إرسال تذاكر PDF** | عند الموافقة على تذكرة ← إنشاء PDF وإرساله للعميل عبر واتساب |

---

## 📁 هيكل المشروع

```
Be Star/
│
├── Be Star Platform/              ← المنصة الرسمية (الجمهور)
│   └── src/
│       ├── pages/                 ← 7 صفحات
│       │   ├── Home.jsx
│       │   ├── About.jsx
│       │   ├── Tickets.jsx
│       │   ├── Speakers.jsx
│       │   ├── Sessions.jsx
│       │   ├── Sponsors.jsx
│       │   └── AIFeatures.jsx
│       └── components/            ← المكونات المشتركة
│           ├── Navbar.jsx
│           ├── Footer.jsx
│           └── ChatWidget.jsx
│
├── admin-frontend/                ← لوحة تحكم الأدمن (الإدارة)
│   └── src/
│       ├── pages/                 ← 15 صفحة إدارية
│       │   ├── Dashboard.jsx
│       │   ├── Tickets.jsx
│       │   ├── PaymentProof.jsx   ← صفحة مراجعة إثبات الدفع
│       │   ├── VipGuests.jsx
│       │   ├── LiveEngagement.jsx
│       │   ├── QuizEngine.jsx
│       │   ├── CertificatesPage.jsx
│       │   ├── ChecklistPage.jsx
│       │   ├── AgendaPage.jsx
│       │   ├── ComplaintsPage.jsx
│       │   ├── Distributors.jsx
│       │   ├── Admins.jsx
│       │   ├── AddAdmin.jsx
│       │   ├── EditAdmin.jsx
│       │   └── Login.jsx
│       └── components/
│           └── Sidebar.jsx
│
├── admin-backend/                 ← السيرفر الخلفي (FastAPI)
│   ├── main.py                    ← نقطة البداية
│   ├── models.py                  ← قاعدة البيانات (SQLAlchemy)
│   ├── routes/                    ← 13 ملف API
│   │   ├── auth.py                ← تسجيل الدخول والمسؤولين
│   │   ├── tickets.py             ← التذاكر
│   │   ├── stats.py               ← إحصائيات الداشبورد
│   │   ├── engagement.py          ← التفاعل المباشر
│   │   ├── quiz.py                ← المسابقات
│   │   ├── certificates.py        ← الشهادات والشكر
│   │   ├── vip.py                 ← ضيوف VIP
│   │   ├── agenda.py              ← الأجندة
│   │   ├── checklist.py           ← قائمة المهام
│   │   ├── complaints.py          ← الشكاوى
│   │   ├── distributors.py        ← الموزعين
│   │   └── chat.py                ← شات بوت API
│   └── services/                  ← الخدمات المساندة
│       ├── whatsapp_service.py    ← خدمة واتساب
│       ├── email_service.py       ← خدمة البريد
│       ├── pdf_generator.py       ← مولد تذاكر PDF
│       ├── certificate_generator.py ← مولد شهادات PDF
│       ├── scoring.py             ← محرك تقييم المسابقات
│       └── n8n_client.py          ← عميل n8n
│
└── n8n_workflow/                  ← سير العمل الآلي
    └── webchat_workflow.json
```

---

> 📅 آخر تحديث: 25 فبراير 2026
> ✍️ النسخة: Human Version (v1.0)
